#include <bits/stdc++.h>
using namespace std;

#define ll long long
#define ll long long
#define ll long long
#define mod 1000000007

void solve(){
	ll n, k;
	cin >> n >> k;
	vector <ll> a(n);
	for(int i = n-1; i >= 0; --i)
		cin >> a[i];
		

}

int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	int t;
	cin >> t;
	while(t--){
		solve();
	}
	return 0;
}
