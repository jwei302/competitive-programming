#include <bits/stdc++.h>
using namespace std;

#define ll long long

const int mxN = 5e3;
int N, a[mxN], X;
int main(){
	ios::sync_with_stdio(0);
	cin.tie(0);
	cin >> N >> X;
	for(int i = 0; i < N; ++i)
		cin >> a[i];

	return 0;
}

